import 'package:flutter/material.dart';

class LineScreen extends StatelessWidget {
  const LineScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Line Screen")),
    );
  }
}
